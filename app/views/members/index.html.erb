<section class="mx-auto max-w-3xl">
  <div class="flex items-center justify-between">
    <div>
      <h1 class="font-display text-xl text-ink">Members</h1>
      <p class="text-sm text-muted"><%= current_organization.name %></p>
    </div>
  </div>

  <% if @can_manage %>
    <div class="mt-6 rounded-large border border-border bg-surface p-5 shadow-soft">
      <h2 class="text-sm font-semibold text-ink">Invite someone</h2>
      <%= form_with url: invitations_path, method: :post, scope: :invitation, class: "mt-3 flex flex-wrap items-end gap-3" do |f| %>
        <div class="flex-1 min-w-48">
          <%= f.label :email, "Email", class: "block text-xs font-medium text-body" %>
          <%= f.email_field :email, required: true, placeholder: "teammate@example.com",
                class: "mt-1 block w-full rounded-lg border-border-strong shadow-soft focus:border-primary-tint0 focus:ring-primary-tint0 sm:text-sm" %>
        </div>
        <div>
          <%= f.label :role, "Role", class: "block text-xs font-medium text-body" %>
          <%= f.select :role, [["Member", "member"], ["Owner", "owner"]],
                {}, class: "mt-1 rounded-lg border-border-strong shadow-soft focus:border-primary-tint0 focus:ring-primary-tint0 sm:text-sm" %>
        </div>
        <%= f.submit "Send invite", class: "cursor-pointer rounded-md bg-primary px-4 py-2 text-sm font-medium text-white hover:bg-primary" %>
      <% end %>
    </div>
  <% end %>

  <div class="mt-6 rounded-large border border-border bg-surface shadow-soft">
    <ul class="divide-y divide-border">
      <% @memberships.each do |membership| %>
        <li class="flex items-center justify-between px-5 py-3">
          <div class="flex items-center gap-3">
            <span class="text-sm font-medium text-ink"><%= membership.user.email %></span>
            <%= rui_badge(text: membership.role, color: membership.owner? ? :primary : :slate, size: :sm) %>
            <% if membership.user_id == current_user.id %><span class="text-xs text-faint">you</span><% end %>
          </div>
          <% if @can_manage && membership.user_id != current_user.id %>
            <%= rui_button_to("Remove", member_path(membership), method: :delete, variant: :link, color: :danger, size: :sm,
                  data: { turbo_confirm: "Remove #{membership.user.email}?" }) %>
          <% end %>
        </li>
      <% end %>

      <% @invitations.each do |invitation| %>
        <li class="flex items-center justify-between px-5 py-3 bg-fill/50">
          <div class="flex items-center gap-3">
            <span class="text-sm text-body"><%= invitation.email %></span>
            <%= rui_badge(text: "invited · #{invitation.role}", color: :warning, size: :sm) %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</section>
