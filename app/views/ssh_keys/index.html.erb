<% content_for :title, "SSH Keys · Conductor" %>

<section class="max-w-7xl mx-auto px-4 py-10">
  <div class="flex items-center justify-between">
    <div>
      <h1 class="font-display text-2xl text-ink tracking-display">SSH Keys</h1>
      <p class="mt-1 text-sm text-muted">Manage SSH keys for server connections.</p>
    </div>
    <%= link_to new_ssh_key_path, class: "inline-flex items-center gap-2 rounded-lg bg-ink px-4 py-2 text-sm font-medium text-white hover:bg-body" do %>
      <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/></svg>
      Add SSH Key
    <% end %>
  </div>

  <% if @ssh_keys.any? %>
    <div class="mt-6 rounded-large border border-border bg-surface shadow-soft overflow-hidden">
      <table class="min-w-full divide-y divide-border">
        <thead class="bg-fill">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-semibold uppercase tracking-wide text-muted">Name</th>
            <th class="px-6 py-3 text-left text-xs font-semibold uppercase tracking-wide text-muted">Type</th>
            <th class="px-6 py-3 text-left text-xs font-semibold uppercase tracking-wide text-muted">Fingerprint</th>
            <th class="px-6 py-3 text-left text-xs font-semibold uppercase tracking-wide text-muted">Servers</th>
            <th class="px-6 py-3 text-right text-xs font-semibold uppercase tracking-wide text-muted">Actions</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-border">
          <% @ssh_keys.each do |ssh_key| %>
            <tr>
              <td class="px-6 py-4">
                <%= link_to ssh_key.name, ssh_key_path(ssh_key), class: "text-sm font-medium text-ink hover:underline" %>
              </td>
              <td class="px-6 py-4 text-sm text-muted">
                <code class="bg-fill-strong px-2 py-1 rounded text-xs"><%= ssh_key.key_type || "unknown" %></code>
              </td>
              <td class="px-6 py-4 text-sm text-muted">
                <code class="text-xs"><%= ssh_key.fingerprint&.truncate(30) || "—" %></code>
              </td>
              <td class="px-6 py-4 text-sm text-muted">
                <%= ssh_key.servers.count %> server<%= ssh_key.servers.count == 1 ? "" : "s" %>
              </td>
              <td class="px-6 py-4 text-right text-sm">
                <%= link_to "Edit", edit_ssh_key_path(ssh_key), class: "font-medium text-body hover:text-ink" %>
                <span class="text-faint mx-2">·</span>
                <%= button_to "Delete", ssh_key_path(ssh_key), method: :delete,
                    data: { turbo_confirm: "Are you sure? This cannot be undone." },
                    class: "font-medium text-danger hover:text-danger-deep" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <div class="mt-6 rounded-large border border-dashed border-border-strong bg-fill p-12 text-center">
      <svg class="mx-auto h-12 w-12 text-faint" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z"/>
      </svg>
      <div class="mt-4 text-sm text-muted">No SSH keys yet.</div>
      <p class="mt-1 text-xs text-faint">Add an SSH key to connect to your servers.</p>
      <%= link_to "Add your first SSH key", new_ssh_key_path, class: "mt-4 inline-block text-sm font-medium text-ink hover:underline" %>
    </div>
  <% end %>

  <div class="mt-8 rounded-card border border-border bg-fill p-5">
    <h3 class="text-sm font-semibold text-body">Why Conductor needs an SSH key</h3>
    <p class="mt-1.5 text-sm text-muted">
      Conductor connects to your servers over SSH to deploy apps, run scripts, collect
      metrics, and take backups — no agent required. An SSH key is a pair: a
      <strong>public</strong> key that lives <em>on the server</em> (it says "trust whoever
      holds the match"), and a <strong>private</strong> key that Conductor holds and keeps
      encrypted at rest. You give Conductor the <em>private</em> key here; you put the
      <em>public</em> key on each server.
    </p>

    <h3 class="mt-4 text-sm font-semibold text-body">Set one up (≈2 minutes)</h3>
    <ol class="mt-2 text-sm text-body space-y-2.5 list-decimal list-inside">
      <li>
        <span class="font-medium">Generate a key pair</span> on your machine — this writes
        <code class="bg-surface px-1.5 py-0.5 rounded text-xs">conductor</code> (private) and
        <code class="bg-surface px-1.5 py-0.5 rounded text-xs">conductor.pub</code> (public):
        <div class="mt-1.5 ml-5"><code class="block bg-surface px-2 py-1 rounded text-xs">ssh-keygen -t ed25519 -C "conductor" -f ~/.ssh/conductor</code></div>
      </li>
      <li>
        <span class="font-medium">Authorize it on the server</span> — copy the
        <em>public</em> key into the server's list of trusted keys. Easiest way:
        <div class="mt-1.5 ml-5"><code class="block bg-surface px-2 py-1 rounded text-xs">ssh-copy-id -i ~/.ssh/conductor.pub root@YOUR_SERVER_IP</code></div>
        <span class="text-xs text-muted">(or manually append <code class="bg-surface px-1 py-0.5 rounded text-2xs">conductor.pub</code>'s contents to <code class="bg-surface px-1 py-0.5 rounded text-2xs">~/.ssh/authorized_keys</code> on the server, as the user Conductor will log in as).</span>
      </li>
      <li>
        <span class="font-medium">Give Conductor the private key</span> — click
        <em>Add SSH Key</em> above and paste the contents of
        <code class="bg-surface px-1.5 py-0.5 rounded text-xs">~/.ssh/conductor</code>
        (the file <em>without</em> <code class="bg-surface px-1 py-0.5 rounded text-2xs">.pub</code>). It's encrypted before it's stored.
      </li>
      <li>
        <span class="font-medium">Attach it to a server</span> — when you add or edit a
        server, pick this key under <em>SSH Configuration</em>. Conductor will use it to
        connect. Hit <em>Test connection</em> to confirm it works.
      </li>
    </ol>

    <p class="mt-4 text-xs text-faint">
      Tip: use one dedicated key for Conductor (not your personal laptop key) so you can
      revoke its access independently — just remove its line from the server's
      <code class="bg-surface px-1 py-0.5 rounded text-2xs">authorized_keys</code>.
    </p>
  </div>
</section>
