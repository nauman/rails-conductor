<% content_for :title, "Edit #{@ssh_key.name} · Conductor" %>

<%= render partial: "shared/page_head",
      locals: { title: "Edit #{@ssh_key.name}.",
                lede: "Replacing the private key means re-authorizing the new public half on every server that used this one." } %>

<section class="max-w-3xl mx-auto px-4 py-8">

  <%= render "form", ssh_key: @ssh_key %>
</section>
