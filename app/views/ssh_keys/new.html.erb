<% content_for :title, "New SSH Key · Conductor" %>

<section class="max-w-2xl mx-auto px-4 py-10">
  <div class="mb-8">
    <h1 class="text-2xl font-semibold text-slate-900">New SSH Key</h1>
    <p class="mt-1 text-sm text-slate-500">Add a private key for server connections.</p>
  </div>

  <%= render "form", ssh_key: @ssh_key %>
</section>
