<%= form_with model: backup, class: "space-y-6" do |f| %>
  <% if backup.errors.any? %>
    <div class="rounded-card border border-danger/30 bg-danger-tint p-4">
      <div class="text-sm font-medium text-danger-deep">Please fix the following errors:</div>
      <ul class="mt-2 list-disc list-inside text-sm text-danger-deep">
        <% backup.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="rounded-large border border-border bg-surface p-6 shadow-soft space-y-6">
    <h3 class="text-sm font-semibold text-ink">Backup Source</h3>
    <p class="text-xs text-muted -mt-4">Choose what to backup - a server's database or an app's database.</p>

    <div class="grid gap-6 sm:grid-cols-2">
      <div>
        <%= f.label :server_id, "Server", class: "block text-sm font-medium text-body" %>
        <%= f.collection_select :server_id, @servers, :id, :name, { include_blank: "Select server (optional)" }, class: "mt-1 block w-full rounded-lg border-border-strong shadow-soft focus:border-primary focus:ring-primary sm:text-sm" %>
      </div>

      <div>
        <%= f.label :app_id, "App", class: "block text-sm font-medium text-body" %>
        <%= f.collection_select :app_id, @apps, :id, :name, { include_blank: "Select app (optional)" }, class: "mt-1 block w-full rounded-lg border-border-strong shadow-soft focus:border-primary focus:ring-primary sm:text-sm" %>
      </div>
    </div>
  </div>

  <div class="rounded-large border border-border bg-surface p-6 shadow-soft space-y-6">
    <h3 class="text-sm font-semibold text-ink">Storage Destination</h3>

    <div class="grid gap-6 sm:grid-cols-2">
      <div>
        <%= f.label :provider, class: "block text-sm font-medium text-body" %>
        <%= f.select :provider, Backup::PROVIDERS.map { |p| [p.titleize.gsub("_", " "), p] }, {}, class: "mt-1 block w-full rounded-lg border-border-strong shadow-soft focus:border-primary focus:ring-primary sm:text-sm" %>
      </div>

      <div>
        <%= f.label :credential_id, "Credential", class: "block text-sm font-medium text-body" %>
        <% if @credentials.any? %>
          <%= f.collection_select :credential_id, @credentials, :id, :name, { include_blank: "Select credential" }, class: "mt-1 block w-full rounded-lg border-border-strong shadow-soft focus:border-primary focus:ring-primary sm:text-sm" %>
        <% else %>
          <div class="mt-1 text-sm text-muted">
            No credentials available. <%= link_to "Add one first", new_credential_path, class: "text-ink hover:underline" %>.
          </div>
        <% end %>
      </div>
    </div>

    <div class="grid gap-6 sm:grid-cols-2">
      <div>
        <%= f.label :bucket_name, class: "block text-sm font-medium text-body" %>
        <%= f.text_field :bucket_name, class: "mt-1 block w-full rounded-lg border-border-strong shadow-soft focus:border-primary focus:ring-primary sm:text-sm font-mono", placeholder: "my-backups-bucket" %>
      </div>

      <div>
        <%= f.label :retention_days, "Retention (days)", class: "block text-sm font-medium text-body" %>
        <%= f.number_field :retention_days, class: "mt-1 block w-full rounded-lg border-border-strong shadow-soft focus:border-primary focus:ring-primary sm:text-sm", min: 1, max: 365, value: backup.retention_days || 7 %>
      </div>
    </div>
  </div>

  <div class="rounded-large border border-border bg-surface p-6 shadow-soft space-y-6">
    <h3 class="text-sm font-semibold text-ink">Schedule</h3>

    <div class="flex items-center gap-3">
      <%= f.check_box :enabled, class: "rounded border-border-strong text-ink focus:ring-primary" %>
      <%= f.label :enabled, "Enable automatic backups", class: "text-sm text-body" %>
    </div>

    <div class="grid gap-6 sm:grid-cols-2">
      <div>
        <%= f.label :schedule, "Frequency", class: "block text-sm font-medium text-body" %>
        <%= f.select :schedule, Backup::SCHEDULES.map { |s| [s.titleize, s] }, {}, class: "mt-1 block w-full rounded-lg border-border-strong shadow-soft focus:border-primary focus:ring-primary sm:text-sm" %>
      </div>

      <% if backup.persisted? && backup.next_run_at %>
        <div>
          <div class="block text-sm font-medium text-body">Next Run</div>
          <div class="mt-1 text-sm text-body"><%= backup.next_run_at&.strftime("%b %d, %Y at %H:%M") || "Not scheduled" %></div>
        </div>
      <% end %>
    </div>
  </div>

  <div class="flex items-center justify-between pt-4">
    <%= link_to "Cancel", backups_path, class: "text-sm font-medium text-body hover:text-ink" %>
    <%= f.submit backup.persisted? ? "Update Backup" : "Create Backup", class: "inline-flex items-center rounded-lg bg-ink px-4 py-2 text-sm font-medium text-white hover:bg-body cursor-pointer" %>
  </div>
<% end %>
