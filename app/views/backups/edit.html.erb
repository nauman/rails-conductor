<% content_for :title, "Edit Backup · Conductor" %>

<section class="max-w-3xl mx-auto px-4 py-10">
  <div class="mb-6">
    <h1 class="text-2xl font-semibold text-slate-900">Edit Backup</h1>
    <p class="mt-1 text-sm text-slate-500"><%= @backup.bucket_name %></p>
  </div>

  <%= render "form", backup: @backup %>
</section>
