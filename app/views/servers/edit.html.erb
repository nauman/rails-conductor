<% content_for :title, "Edit #{@server.name} · Conductor" %>

<section class="max-w-2xl mx-auto px-4 py-10">
  <div class="mb-8">
    <h1 class="text-2xl font-semibold text-slate-900">Edit Server</h1>
    <p class="mt-1 text-sm text-slate-500">Update server configuration.</p>
  </div>

  <%= render "form", server: @server %>
</section>
