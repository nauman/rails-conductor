<% content_for :title, "Edit #{@server.name} · Conductor" %>

<%= render partial: "shared/page_head", locals: { title: "Edit #{@server.name}.", lede: "Metrics are measured, not entered — they refresh on connect." } %>

<section class="max-w-3xl mx-auto px-4 py-8">

  <%= render "form", server: @server %>
</section>
