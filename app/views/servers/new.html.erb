<% content_for :title, "New Server · Conductor" %>

<section class="max-w-2xl mx-auto px-4 py-10">
  <div class="mb-8">
    <h1 class="font-display text-2xl text-ink tracking-display">Add a server</h1>
    <p class="mt-1 text-sm text-muted">Register an existing server so Conductor can monitor and deploy to it over SSH.</p>
  </div>

  <%= render "form", server: @server %>
</section>
