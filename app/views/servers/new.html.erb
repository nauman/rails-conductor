<% content_for :title, "New Server · Conductor" %>

<%= render partial: "shared/page_head", locals: { title: "Add a server you already own.", lede: "Conductor reaches it over plain SSH — no agent to install. Name, IP and a key is all it needs; CPU, memory, disk and status fill themselves in on first connect." } %>

<section class="max-w-3xl mx-auto px-4 py-8">

  <%= render "form", server: @server %>
</section>
