<% content_for :title, "New Server · Conductor" %>

<section class="max-w-2xl mx-auto px-4 py-10">
  <div class="mb-8">
    <h1 class="font-display text-2xl text-ink tracking-display">New Server</h1>
    <p class="mt-1 text-sm text-muted">Add a new VM to monitor.</p>
  </div>

  <%= render "form", server: @server %>
</section>
