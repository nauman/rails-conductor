<% content_for :title, "Edit #{@script.name}" %>
<%= render partial: "shared/page_head", locals: { title: "Edit #{@script.name}.", lede: "Shell that Conductor runs over SSH. Keep it idempotent — it may run again." } %>

<div class="max-w-3xl mx-auto px-4 py-8">
  <%= render "form", script: @script %>
</div>
