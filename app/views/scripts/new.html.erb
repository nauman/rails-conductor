<% content_for :title, "New Script" %>
<%= render partial: "shared/page_head", locals: { title: "New script.", lede: "Shell that Conductor runs over SSH. Keep it idempotent — it may run again." } %>

<div class="max-w-3xl mx-auto px-4 py-8">
  <%= render "form", script: @script %>
</div>
