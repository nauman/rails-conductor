<% content_for :title, "Edit #{@app.name} · Conductor" %>

<%= render partial: "shared/page_head", locals: { title: "Edit #{@app.name}.", lede: "Changes take effect on the next deploy unless noted." } %>

<section class="max-w-3xl mx-auto px-4 py-8">

  <%= render "form", app: @app, servers: @servers %>
</section>
