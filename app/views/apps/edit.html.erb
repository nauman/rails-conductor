<% content_for :title, "Edit #{@app.name} · Conductor" %>

<section class="max-w-2xl mx-auto px-4 py-10">
  <div class="mb-8">
    <h1 class="font-display text-2xl text-ink tracking-display">Edit App</h1>
    <p class="mt-1 text-sm text-muted">Update app configuration.</p>
  </div>

  <%= render "form", app: @app, servers: @servers %>
</section>
