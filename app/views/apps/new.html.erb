<% content_for :title, "New App · Conductor" %>

<%= render partial: "shared/page_head", locals: { title: "Register an app.", lede: "Point Conductor at a repository and a server. It handles the deploy, the routing, and the backups from there." } %>

<section class="max-w-3xl mx-auto px-4 py-8">

  <%= render "form", app: @app, servers: @servers %>
</section>
