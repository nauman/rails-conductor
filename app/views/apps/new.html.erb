<% content_for :title, "New App · Conductor" %>

<section class="max-w-2xl mx-auto px-4 py-10">
  <div class="mb-8">
    <h1 class="font-display text-2xl text-ink tracking-display">New App</h1>
    <p class="mt-1 text-sm text-muted">Register a deployed Rails application.</p>
  </div>

  <%= render "form", app: @app, servers: @servers %>
</section>
