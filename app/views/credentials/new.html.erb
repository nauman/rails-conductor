<% content_for :title, "New Credential · Conductor" %>

<section class="max-w-2xl mx-auto px-4 py-10">
  <div class="mb-8">
    <h1 class="text-2xl font-semibold text-slate-900">New Credential</h1>
    <p class="mt-1 text-sm text-slate-500">Add API keys for external services.</p>
  </div>

  <%= render "form", credential: @credential %>
</section>
