<% content_for :title, "New Credential · Conductor" %>

<%= render partial: "shared/page_head", locals: { title: "Connect a service.", lede: "Storage for backups, DNS for routing, a registry for images. Verify it after saving so Conductor knows the token actually works." } %>

<section class="max-w-3xl mx-auto px-4 py-8">

  <%= render "form", credential: @credential %>
</section>
