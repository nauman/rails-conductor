<% content_for :title, "Edit #{@credential.name} · Conductor" %>

<%= render partial: "shared/page_head", locals: { title: "Edit #{@credential.name}.", lede: "Re-verify after changing a key — a stale verification is worse than none." } %>

<section class="max-w-3xl mx-auto px-4 py-8">

  <%= render "form", credential: @credential %>
</section>
