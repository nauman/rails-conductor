<% content_for :title, "Edit #{@credential.name} · Conductor" %>

<section class="max-w-2xl mx-auto px-4 py-10">
  <div class="mb-8">
    <h1 class="text-2xl font-semibold text-slate-900">Edit Credential</h1>
    <p class="mt-1 text-sm text-slate-500">Update API keys and settings.</p>
  </div>

  <%= render "form", credential: @credential %>
</section>
